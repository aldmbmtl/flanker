# StubVisualSync.gd
# Index of visual-sync stub helpers used in Tier 2 RPC tests.
# Each stub is in its own file with a global class_name for direct use in tests.
#
#   StubMinionSpawner  — tests/helpers/StubMinionSpawner.gd
#   StubBuildSystem    — tests/helpers/StubBuildSystem.gd
#   StubTreePlacer     — tests/helpers/StubTreePlacer.gd
#   StubFogOverlay     — tests/helpers/StubFogOverlay.gd
#   StubRemotePlayerManager — tests/helpers/StubRemotePlayerManager.gd
#
# This file intentionally contains no runnable code.
extends RefCounted
